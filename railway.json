{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && mkdir -p static && cp -r dist/* static/ && echo 'Static files copied' && ls -la static/ && pip install -r requirements.txt && pip install gunicorn"
  },
  "deploy": {
    "startCommand": "sh -c 'FLASK_ENV=production gunicorn --config gunicorn.conf.py wsgi:app'",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
